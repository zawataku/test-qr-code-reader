/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // swcMinify: true,
    experimental: {
        // 必要に応じて Turbopack を有効化
        // turboMode: true,
    },
};

module.exports = nextConfig;
